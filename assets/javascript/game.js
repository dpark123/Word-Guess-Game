document.onkeyup = function(event){
    console.log("you pressed a button");
    console.log(event.key);//event can be named anything but needs to be the same in function declaration and when called.
   }